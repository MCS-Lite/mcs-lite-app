import { default as React, PropTypes } from 'react';
import { renameProp, compose, pure, setPropTypes, defaultProps } from 'recompose';
import { default as Card } from '../common/card/';
import { default as DatachannelInfo } from '../common/datachannelInfo/';
import { default as Display, propTypes as DisplayPropTypes } from '../float/Display';

const displayCard = ({
  numberOfCards,
  name,
  description,
  dataChnId,
  configs,
  fullScreenConfig,
  detailButtonConfig,
  recordedAt,
  ...props,
}) => (
  <Card
    fullScreenConfig={fullScreenConfig}
    configs={configs}
    detailButtonConfig={detailButtonConfig}
    numberOfCards={numberOfCards} >
    <div>
      <Display {...props} />
      <DatachannelInfo
        name={name}
        dataChnId={dataChnId}
        recordedAt={recordedAt}
        description={description}
      />
    </div>
  </Card>
);

export default compose(
  defaultProps({
    numberOfCards: 2,
    displayType: 'none',
  }),
  setPropTypes({
    configs: PropTypes.object.isRequired,
    numberOfCards: PropTypes.number,
    name: PropTypes.string.isRequired,
    description: PropTypes.string.isRequired,
    dataChnId: PropTypes.string.isRequired,
    fullScreenConfig: PropTypes.object,
    recordedAt: PropTypes.number,
    values: PropTypes.object,
    detailButtonConfig: PropTypes.object,
    ...DisplayPropTypes,
  }),
  pure,
  renameProp('values', 'status'),
)(displayCard);
