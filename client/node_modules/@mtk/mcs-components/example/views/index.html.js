import { default as React } from 'react';
import {
  WebpackScriptEntry,
  WebpackStyleEntry,
} from 'reacthtmlpack/lib/entry';

export default (
  <html lang="en">
    <head>
      <meta charSet="UTF-8" />
      <title>MCS Linkit smart 7688 video streaming service</title>
    </head>
    <body style={{ background: '#000' }}>
      <div id="app" />
      <WebpackScriptEntry
        chunkName="client"
        chunkFilepath="../entry.js"
        configFilepath="../webpack.config.js" />
    </body>
  </html>
);

