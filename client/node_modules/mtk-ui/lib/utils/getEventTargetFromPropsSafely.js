"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = getEventTargetFromPropsSafely;
/**
 * geteventTargetFromPropsSafely()
 *
 * @input props
 *
 * @return (props.eventTarget(props) || null)
 *
 * ref: https://github.com/react-bootstrap/react-overlays/blob/master/src%2FPosition.js
 */
function getEventTargetFromPropsSafely() {
  var props = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};

  if (!props.eventTarget) {
    return null;
  }

  var eventTarget = props.eventTarget(props);
  if (!eventTarget) {
    // This is so we can just use === check below on all falsy eventTargets.
    return null;
  }

  return eventTarget;
}
module.exports = exports['default'];