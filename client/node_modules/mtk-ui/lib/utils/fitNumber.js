'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = fitNumber;

var _flowRight = require('lodash/flowRight');

var _flowRight2 = _interopRequireDefault(_flowRight);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/**
 * Fit a number according to provided min, max and step
 *
 * @param {min} number
 * @param {max} number
 * @param {value} number
 * @return {number} if min <= value <= max return value, else return the limitiation exceeded by the value
 *
 */
function fitNumber(value, min, max, step) {
  return (0, _flowRight2.default)(function (val) {
    return Number.isFinite(step) ? Math.round(val / step) * step : val;
  }, function (val) {
    return Number.isFinite(max) ? Math.min(max, val) : val;
  }, function (val) {
    return Number.isFinite(min) ? Math.max(min, val) : val;
  })(value);
}
module.exports = exports['default'];