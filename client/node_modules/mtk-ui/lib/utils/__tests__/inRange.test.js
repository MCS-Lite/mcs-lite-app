'use strict';

var _inRange = require('../inRange');

var _inRange2 = _interopRequireDefault(_inRange);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var testRange = (0, _inRange2.default)({ start: 0, end: 10 });

describe('utils/inRange', function () {
  it('should return true when value is between start and end value', function () {
    var result = testRange(5);
    expect(result).to.be.true;
  });

  it('should return true when value equal start value', function () {
    var result = testRange(0);
    expect(result).to.be.true;
  });

  it('should return true when value equal end value', function () {
    var result = testRange(10);
    expect(result).to.be.true;
  });

  it('should return false when value greater than end value', function () {
    var result = testRange(10.1);
    expect(result).to.be.false;
  });

  it('should return false when value less than start value', function () {
    var result = testRange(-1);
    expect(result).to.be.false;
  });
});