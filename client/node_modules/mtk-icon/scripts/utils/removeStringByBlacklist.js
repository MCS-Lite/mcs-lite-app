import { curry } from 'lodash';

const removeStringByBlacklist = (
  blacklist = [],
  string,
) =>
  blacklist.reduce(
    (result, pattern) => result.replace(pattern, ''),
    string
  );

export default curry(removeStringByBlacklist, 2);
